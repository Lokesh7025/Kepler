{
  "name": "@kepler/protocol",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Dependency-free event and RPC definitions for Kepler",
  "license": "Apache-2.0",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -b tsconfig.build.json --force",
    "test": "node --test test/*.test.ts",
    "typecheck": "tsc --noEmit"
  }
}
